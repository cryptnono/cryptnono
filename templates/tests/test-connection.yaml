apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bloodcoins.fullname" . }}-test-connection"
  labels:
    {{- include "bloodcoins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bloodcoins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
